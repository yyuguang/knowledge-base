---
type: concept
tags:
  - java
  - jvm
  - gc
summary: "HotSpot 虚拟机各代垃圾收集器的特性、原理与选型"
sources:
  - "raw/books/深入理解Java虚拟机：JVM高级特性与最佳实践（第3版）周志明.pdf"
aliases:
  - GC收集器
  - 垃圾回收器
status: evolving
confidence: 0.9
updated: "2026-04-27 22:30:00"
---

# 概念_垃圾收集器

## 定义

垃圾收集器是垃圾收集算法的具体实践者。《Java虚拟机规范》对收集器如何实现无强制规定，各厂商的虚拟机实现各有差异。HotSpot 提供多款收集器以适应不同场景。

## 经典收集器（JDK 7–10）

### 新生代
- **Serial**：单线程，STW，客户端模式默认，简单高效
- **ParNew**：Serial 的多线程版，只能配合 CMS
- **Parallel Scavenge**：吞吐量优先，自适应调节（GC Ergonomics），-XX:MaxGCPauseMillis / -XX:GCTimeRatio

### 老年代
- **Serial Old**：单线程，标记-整理，CMS 后备预案
- **Parallel Old**：Parallel Scavenge 的老年代搭档，多线程标记-整理
- **CMS（Concurrent Mark Sweep）**：低延迟，6 阶段执行（初始标记→并发标记→重新标记→并发清除→重置），增量更新解决并发标记，Concurrent Mode Failure 退化 Serial Old

### G1（Garbage First）
- Region 化堆（1MB–32MB），新生代/老年代不再物理连续
- **RSet**：每个 Region 记录谁引用自己，避免全堆扫描
- 停顿可预测（-XX:MaxGCPauseMillis），基于回收集合（CSet）和 SATB
- **JDK 9 起成为默认收集器**

## 低延迟收集器（JDK 11+）

| 收集器 | 核心原理 | 特点 |
|--------|----------|------|
| **Shenandoah** | 连接矩阵 + 并发回收 + 读屏障 | 停顿与堆大小无关 |
| **ZGC** | 染色指针（Colored Pointer）+ 读屏障 + Region 动态大小 | 几乎无停顿，亚毫秒级 |
| **Epsilon** | 无操作 | 不回收，仅用于性能测试 |

## 使用场景（选型要点）

- **延迟敏感** → CMS / G1 / ZGC / Shenandoah
- **吞吐量优先** → Parallel Scavenge + Parallel Old
- **小内存/客户端** → Serial

## 在本知识库中的应用

- 为 Java 应用选择收集器时参考：Web 服务选 G1/ZGC，批处理选 Parallel
- 分析 GC 日志时识别收集器：`[ParNew`、`[CMS-concurrent-mark`、`[G1Ergonomics`
- 与 [[concepts/java/概念_垃圾收集算法]] 联动：理解各收集器的算法选择差异

## 关联

- [[concepts/java/概念_垃圾收集算法]] — 各收集器是算法的具体实现者：CMS 用标记-清除、G1 用标记-整理、新生代用标记-复制
- [[concepts/java/概念_JVM内存区域]] — 收集器的工作对象是堆内存中的新生代/老年代，G1 和 ZGC 还引入了 Region 新概念
- [[concepts/java/概念_JIT编译优化技术]] — 锁消除等编译优化降低同步开销，间接影响并发收集器的线程协作效率
